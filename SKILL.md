# crap4ts

Use `crap4ts` as the TypeScript CRAP quality gate in SwarmForge.

## Install

```bash
npm install --global github:lukasa1993/crap4ts
```

## Verify

Run `crap4ts --help` from the project root. Keep generated coverage under `target/`. Use `--fail-over 6` for the SwarmForge cleanup gate.
